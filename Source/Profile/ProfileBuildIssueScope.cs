namespace CeleTech.ShuttleExtension.ModularShuttle.Profile
{
    /// <summary>
    /// Broad scope for a structured profile-build diagnostic.
    /// </summary>
    public enum ProfileBuildIssueScope
    {
        Assembly = 0,
        Segment = 1,
        Module = 2,
        Profile = 3
    }
}
